In order to use the tests, you need to download ChiVO bindata, and make a link 
into the root directory called bindata. 
For example:

cd $HOME/
git clone git@github.com:ChileanVirtualObservatory/bindata.git
cd bindata
git lfs fetch
git lfs checkout
cd $path/ACALIB
ln -s $HOME/bindata .


Remember that you need "git lbf"n for
downloading the bindata. 
